services:
  - type: web
    name: local-qa-app
    env: python
    region: singapore   # 変更したい場合は "oregon" や "frankfurt" も可
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"